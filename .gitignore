# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# desktop-electron (Electron wrapper, lives here so it ships alongside the
# web app but isn't part of it — its own deps/build output stay out of the
# Next.js dev server's file watcher and out of the web app's git tracking)
desktop-electron/node_modules/
desktop-electron/dist/

desktop-electron/backups/

scratch/
